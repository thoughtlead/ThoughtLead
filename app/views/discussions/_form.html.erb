
<% form_for @discussion do | f | %>
  <%= f.error_messages %>

  <%= f.label(:title) %>
  
  <%= f.text_field :title, :class => "text single"  %>
  
  
  <%= f.label(:body) %>
  
  <%= f.text_area(:body, :rows => 18, :class => "text large") %>
  
  
  
  <%= submit_tag "Save" %> or <%= link_to "Cancel", discussions_url %>
  
<% end %>