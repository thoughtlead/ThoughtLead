<% @current_tab = :'Your Profile' %> <!-- TODO  Should only display if current user, otherwise show something like "_____'s Profile"  -->

<div id="primary">
  <h1 class="grouped"><%=h @user %></h1>
  <div class="email_user">
    <%= link_to "Email #{h(@user)}", email_user_url(@user) %>
  </div>

</div>