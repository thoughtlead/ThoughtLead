<% @current_tab = :People %>

<div id="primary">
  <h1>Connect with Other Users</h1>
  <ul class="user_list">
    <% for user in @users %>
      <li class="user">
        <%= avatar_for(user, :small) %>
        <%= link_to h(user), user %>
      </li>
    <% end %>
  </ul>
</div>